# Dev: leave empty → frontend calls same-origin /api/* → Vite proxy (see vite.config.ts) forwards to backend.
# Prod: set your API base, e.g. VITE_API_BASE_URL=https://your-api.com/api
VITE_API_BASE_URL=
